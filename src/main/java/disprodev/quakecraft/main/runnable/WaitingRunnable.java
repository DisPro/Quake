package disprodev.quakecraft.main.runnable;

import disprodev.quakecraft.main.Main;
//import disprodev.quakecraft.main.manager.GameManager;
import org.bukkit.Bukkit;
import org.bukkit.Sound;
import org.bukkit.entity.Player;
import org.bukkit.scheduler.BukkitRunnable;

public class WaitingRunnable {
/*
    public static int timer = 30;

    @Override
    public void run() {

        timer--;

        if (timer == 15 || timer == 10 || timer == 5 || timer == 4 || timer == 3 || timer == 2 || timer == 1) {

            Bukkit.getOnlinePlayers().forEach(p -> {

                p.sendMessage("§eCommencement dans §c" + timer + " §eseconde(s) !");
                p.playSound(p.getLocation(), Sound.CHICKEN_EGG_POP, 1f, 1f);

            });

        } else if (timer == 0) {

            this.cancel();
            timer = 30;
            for (Player players : Bukkit.getOnlinePlayers()) {

                new GameManager(players).loadGame();

            }

        }

    }
*/
}
